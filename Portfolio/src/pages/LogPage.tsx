import { Log } from '../components/Log';

export const LogPage = () => {
    return (
        <div className="pt-20"> {/* Add padding for fixed navbar */}
            <Log />
        </div>
    );
};
